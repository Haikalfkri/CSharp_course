namespace arithmeticOprators
{
    class Program
    {
        static void Main(string[] args)
        {
            int num = 10;

            // addition
            // num = num + 2;
            // num += 2;
            // num++; // increment by 1

            // subtraction
            // num = num - 2;
            // num -= 2;
            // num--; // decrement by 1

            // multiplication
            // num = num * 2;
            // num *= 2;

            // division
            // num = num / 2;
            // num /= 2;

            // modulus
            // num = num % 2;
            // num %= 2;
        }
    }
}